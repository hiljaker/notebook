const auth_routes = require('./auth_routers');
const content_routes = require('./content_routers');
const admin_routes = require('./admin_routers');

module.exports = {
    auth_routes,
    content_routes,
    admin_routes
};
